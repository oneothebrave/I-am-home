# 2026-09-08 重构记录

基于 `84f276c` 的审查，按告警、状态持久化、事件协议和原生队列的顺序实施。本次没有更换 React Native + Swift 技术路线，也没有引入数据库服务。

## 完成情况

| 工作 | 当前状态 |
| --- | --- |
| 统一告警归约、SOS 独立升级、按接收人追踪 | 已实现，Node 回归测试通过 |
| 纯 reducer、加载协调、串行保存、降级/重试 | 已实现，故障与竞态测试通过 |
| React StrictMode 加载、规则表单草稿 | 组件测试通过，原生控件被测试替身替换 |
| v2 schema、旧数据迁移、完整日期、稳定 ID | 已实现，校验和跨午夜迁移测试通过 |
| 原生事件先落盘再确认消费 | JS 侧测试通过；Swift 队列源码及 XCTest 已提供 |
| 原生定位质量过滤、持久配置、启动恢复入口 | 已接入 iOS target、通过 Mac 编译和 XCTest；待继续真机验证 |
| 家外长时间无明显移动执行器 | 原生状态机、位置/运动融合、持久恢复和单次去重已实现；待长时间真机实测 |
| 类型检查、iOS JS 发布包、Windows CI 配置 | 本地检查已执行；远端 CI 尚未运行 |

## 已覆盖的审查问题

- R01/R02：SOS 不会被普通信号覆盖，主动求助直接开启家人通知队列；被动告警转 SOS 会建立新的告警 ID。
- R03/R04：通知按联系人和告警 ID 去重，删除/重排联系人不会漏掉下一位；空名单阻断，完成状态不重复生成动作。
- R05/R06：区分持久保存和内存降级，成功读取刷新副本；读取期间求助保留，配置等待加载，保存失败可重试，重置和写入使用同一协调流程。
- R07/R09/R12：运行时校验、版本检查、时间迁移和输入边界；时间编辑不再逐字写入正式配置。
- R08：位置、电量和原因来自统一状态，不退回硬编码果园或默认电量；电量 0% 可以正常展示。
- R10/R11：原生定位过滤、稳定事件 ID、持久队列、围栏配置恢复和独立启动入口已写入源码，实际后台可靠性仍待真机验证。

额外回归覆盖了保存 Promise 即将完成时又收到新操作的竞态：用例先复现失败，修复后 `flush()` 会等待较新的修改完成。

## 运行验证

Windows 基线验证：`npm ci`、`npm run typecheck`、`npm run bundle:ios` 均通过。2026-09-21 在 macOS 上用 Node 26、Xcode 27 和 iOS 27 模拟器重复执行检查；当前 `npm test` 共 60 项全部通过，iOS Debug 编译与 10 项 Swift XCTest 也全部通过。远端 GitHub Actions 尚未在本文档中记录结论。

```powershell
npm ci
npm run typecheck
npm test
npm run bundle:ios
```

测试在 `tests`，使用 Node 内置测试运行器并加载实际 TS 模块。存储故障用注入的驱动控制，不需要原生环境。组件测试使用与 React 19.1.1 匹配的 `react-test-renderer`，该工具会输出弃用提示；这里只用于开发测试，不能证明真实页面布局或设备表现。

构建配套 `@react-native/babel-preset` 和 `@react-native/metro-config` 已固定为 0.82.0，与 `react-native` 对齐。CI 使用 Node 24，配置参照 [checkout](https://github.com/actions/checkout) 和 [setup-node](https://github.com/actions/setup-node) 的官方接口。

原 [审查报告](reviews/2026-09-08-review.md) 保留历史发现和当时的 24 项诊断结果。原诊断入口已转接到当前正式测试，避免继续运行已过期的接口假设。

## 仍待开发或验证

默认页面明确为演示模式，不发送真实通知。设备模式已开放真实守护开关：开启前检查真实地点、Always 定位、精确位置和围栏同步，启停操作串行调用 Swift，并在原生确认后更新 JS；启动、回前台和原生错误时会回读状态。仓库现已包含完整 Xcode workspace，`GuardianBootstrap.restore()` 已在 React Native 启动前接入 AppDelegate；这只证明工程集成与模拟器启动，不证明后台恢复已在真机生效。

Xcode 27 开始要求使用 UIKit scene 生命周期，工程已通过 `SceneDelegate` 和 `UIApplicationSceneManifest` 完成迁移。React Native 0.82 所带 `fmt` 在 Apple Clang 21 下还需要 Podfile 中的兼容补丁；`pod install` 会可重复地应用该补丁，并将所有 Pods 的最低部署版本对齐到 React Native 支持范围。

真实设备模式现在可以清除演示数据、读取定位/精确位置/运动权限、获取一次真实位置、同步原生围栏并启停后台守护。定位样本必须在 2 分钟内且水平精度不超过 100 米；围栏和启停操作均串行处理快速变化，并暴露失败、回滚与重试。家外长时间无明显移动执行器已接入原生位置、活动和步数信号，每日单段守护时间会限制该规则的累计，且不向本人发送本地确认通知。尚未完成低电量的正式后台执行器、家人 APNs/自动短信和送达回执；本次家外停留规则及守护时段边界也仍需长时间真机实测。

旧 `HH:mm` 记录的原始日期无法精确恢复，迁移按保存时间推断并保留说明。未带接收人 ID 的旧演练通知不会被猜测为已通知某位家人。
